<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sharehouse.dao.MyPageDao">

	<select id="apply" resultType="com.sharehouse.dto.MyPageDto">
		select u.id, u.name, u.email, u.tel, o.title from applyoffering a inner join 
		user u on a.id = u.id inner join offering o on a.id = o.id where u.id = #{id}
	</select>
		
	<select id="room" resultType="com.sharehouse.dto.MyPageDto">
		select a.id, r.room_name from room_info r inner join applyoffering a on r.board_no = a.board_no
		where a.id = #{id}
	</select>
	
	<delete id="delete"  parameterType="int">
		delete from apply where id = #{id}
	</delete>
	
</mapper>
